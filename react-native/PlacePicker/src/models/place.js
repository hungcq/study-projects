export default class PlaceInfo {
    constructor(id, name, address, photoRef) {
        this.id = id;
        this.name = name;
        this.address = address;
        this.photoRef = photoRef
    }
}